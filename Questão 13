package main
import "fmt"

func main() {
  var nota float32
  var conceito rune
  
fmt.Println("Digite a nota do aluno: ")
fmt.Scan(&nota)

switch {
    case nota >= 9 && nota <= 10:

   conceito = 'A'
    fmt.Printf("A nota foi: %.2f. O conceito foi: %c\n", nota, conceito)
    
    case nota < 9 && nota >= 7.5:
    
    conceito = 'B'
    fmt.Printf("A nota foi: %.2f. O conceito foi: %c\n", nota, conceito)
    
    case nota < 7.5 && nota >= 6:
    
    conceito = 'C'
    fmt.Printf("A nota foi: %.2f. O conceito foi: %c\n", nota, conceito)
    
    case nota < 6 && nota >= 0:
    
    conceito = 'D'
    fmt.Printf("A nota foi: %.2f. O conceito foi: %c\n", nota, conceito)
    
    default:
    fmt.Println("Nota inválida ")
}
}
