package main

import "fmt"

func main() {
	var a1, r, n int

	fmt.Scan(&a1, &r, &n)

	soma := 0
	
	for i := 0; i < n; i++ {
	
		termoAtual := a1 + (i * r)
	
		soma += termoAtual
	}
	fmt.Println(soma)
}
